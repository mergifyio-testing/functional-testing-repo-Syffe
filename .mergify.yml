pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20231213110655/TQAWPEItest_draft_pr_summary_when_queue_frozen/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
