pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20220825151014/test_train_reset_after_unexpected_base_branch_changes/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_max_wait_time: 0 s
  batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
