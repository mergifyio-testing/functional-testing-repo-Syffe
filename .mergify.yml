pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20221226141403/test_train_reset_after_pr_unexpected_updated_changes/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: true
  batch_max_wait_time: 0 s
  batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
