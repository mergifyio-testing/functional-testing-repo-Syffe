pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230905132021/test_unqueue_report_test/main
  name: Merge default
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
