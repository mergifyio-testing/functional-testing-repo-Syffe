pull_request_rules:
- - actions:
      queue:
        name: default
    conditions:
    - base=20230321165535/test_queued_label/main
    name: Queue
  - actions:
      label:
        toggle:
        - queued
    conditions:
    - queue-position>=0
    name: label
queue_rules:
- merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
