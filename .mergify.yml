pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230215150903/test_freeze_eventlogs/main
  - label=queue
  name: queueit
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
